# About dpkg
# dpkg is the software at the base of the package management system in the free 
# operating system Debian and its numerous derivatives. 
# dpkg is used to install, remove, and provide information about .deb packages.
#
# References:
# https://en.wikipedia.org/wiki/Dpkg


# Install the package or upgrade it
dpkg -i test.deb

# Remove a package including configuration files
dpkg -P test.deb

# List all installed packages with versions and details
dpkg -I

# Find out if a Debian package is installed or not
dpkg -s test.deb | grep Status
